import { Dashboard } from '@/components/Dashboard'

export const metadata = {
  title: 'Dashboard - PollsIA',
  description: 'Painel de controle para gerenciamento de pools de liquidez',
}

export default function DashboardPage() {
  return <Dashboard />
}